# Vim エディタ 使い方ガイド (Vim Editor Guide)

## 1. Vimの4つのモード (Modes)
Vimは他のエディタとは異なり、複数の「モード」を持っています。
- **ノーマルモード (Normal Mode)**: Vimを起動した時の基本的なモードです。カーソルの移動、コピー、貼り付け、削除などのコマンドを実行します。（他のモードで `ESC` を押すと、常にノーマルモードに戻ります。）
- **インサートモード (Insert Mode)**: テキストを入力するモードです。ノーマルモードで `i`, `a`, `o` などを押すとこのモードに入ります。
- **コマンドラインモード (Command-line Mode)**: ファイルの保存、終了、検索と置換、環境設定などのコマンドを処理するモードです。ノーマルモードで `:` を押すと、画面の一番下にプロンプトが開き、このモードに入ります。
- **ビジュアルモード (Visual Mode)**: テキストをブロック指定して選択するモードです。ノーマルモードで `v` (文字単位)、`V` (行単位)、`Ctrl+v` (矩形/ブロック単位) を押してこのモードに入ります。

---

## 2. モードの切り替え方法
- **ノーマルモードに戻る**: `ESC` または `Ctrl + [`
- **インサートモードに入る**: 
  - `i`: 現在のカーソル位置の直前から入力 (insert)
  - `I`: 現在の行の先頭（空白を除く）から入力
  - `a`: 現在のカーソル位置の直後から入力 (append)
  - `A`: 現在の行の末尾から入力
  - `o`: 現在の行のすぐ下に新しい空行を作り、そこから入力
  - `O`: 現在の行のすぐ上に新しい空行を作り、そこから入力

---

## 3. カーソルの移動 (ノーマルモード)
方向キー(←, ↓, ↑, →)の代わりに以下のキーを使うことで、手をキーボードの中央に保ったまま素早く移動できます。
- **基本移動**: 
  - `h`: 左
  - `j`: 下
  - `k`: 上
  - `l`: 右
- **単語単位の移動**:
  - `w`: 次の単語の先頭に移動
  - `b`: 前の単語の先頭に移動
  - `e`: 単語の末尾に移動
- **行内の移動**:
  - `0`: 現在の行の一番先頭に移動
  - `^`: 現在の行の最初の空白ではない文字に移動
  - `$`: 現在の行の末尾に移動
- **文書内の移動**:
  - `gg`: 文書の一番最初の行に移動
  - `G`: 文書の一番最後の行に移動
  - `[数字]G`: 入力した数字の行に移動 (例: `50G` - 50行目に移動)

---

## 4. テキスト編集 (削除、コピー、貼り付け、置換)
- **削除および切り取り (Delete)**:
  - `x`: 現在カーソルがある位置の文字を1つ削除
  - `dd`: 現在の行全体を削除（切り取りに該当し、後で貼り付け可能）
  - `dw`: 現在のカーソル位置から単語の終わりまで削除
  - `d$`: 現在のカーソル位置から行の末尾まで削除
- **コピー (Yank)**:
  - `yy`: 現在の行全体をコピー
  - `yw`: 現在の位置から単語の終わりまでコピー
- **貼り付け (Put)**:
  - `p`: カーソルの次（右の文字または下の行）に貼り付け
  - `P`: カーソルの前（左の文字または上の行）に貼り付け
- **変更 (Change - 削除後すぐにインサートモードへ切り替え)**:
  - `cw`: 単語の終わりまで削除してインサートモードに切り替え
  - `cc`: 現在の行全体を削除してインサートモードに切り替え

---

## 5. 取り消し（Undo）とやり直し（Redo）
- `u`: 直前の操作を取り消す (Undo)
- `Ctrl + r`: 取り消した操作をやり直す (Redo)

---

## 6. 検索と置換
- **検索**:
  - `/検索語`: 現在のカーソル位置から下方向へ「検索語」を探す
  - `?検索語`: 現在のカーソル位置から上方向へ「検索語」を探す
  - `n`: 次の検索結果へ移動（検索方向を維持）
  - `N`: 前の検索結果へ移動（検索方向の逆）
- **置換 (コマンドラインモード)**:
  - `:s/old/new/g`: カーソルがある現在の行で、すべての'old'文字列を'new'単語に置換
  - `:%s/old/new/g`: ファイル全体で、すべての'old'文字列を'new'単語に置換
  - `:%s/old/new/gc`: ファイル全体で置換を行うが、各置換の前に確認(y/n)を求める

---

## 7. 画面スクロール操作
- **画面スクロール**:
  - `Ctrl + f`: 1画面分下にスクロール (Forward / Page Down)
  - `Ctrl + b`: 1画面分上にスクロール (Backward / Page Up)
  - `Ctrl + d`: 半画面分下にスクロール (Down)
  - `Ctrl + u`: 半画面分上にスクロール (Up)

---

## 8. 保存と終了 (コマンドラインモード)
- `:w`: 変更を保存 (write)
- `:q`: 終了 (quit、変更がある場合は終了されません)
- `:wq`: 保存して終了
- `:x`: 保存して終了 (`:wq`と同様ですが、ファイルが変更されている場合のみ保存操作を行います)
- `:q!`: 変更を無視して強制終了

---

## 9. 実務活用シナリオ (Practical Scenarios)

### シナリオ1: テキストファイル全体で特定の単語を一括変更したい時
- **状況**: ドキュメントやコード全体の「apple」という単語をすべて「banana」に一度に変更したい。
- **操作**: `ESC`を押してノーマルモードであることを確認した後、`:%s/apple/banana/g` と入力して `Enter` を押します。

### シナリオ2: 現在の行をコピーして複数回連続で貼り付けたい時
- **状況**: 設定値など似たような形式のコードを複数作るために、今いる行をコピーして下にさらに3行貼り付けたい。
- **操作**: ノーマルモードで `yy` を押して現在の行をコピーした後、`p`を3回押すか、`3p` と入力して3回貼り付けます。

### シナリオ3: カーソルを移動しながら単語をまとめて素早く削除したい時
- **状況**: 現在のカーソル位置から後ろにある3つの単語（変数名など）が不要なので一度に削除したい。
- **操作**: ノーマルモードで `3dw` を押すと、現在のカーソル位置から3つの単語がきれいに削除されます。

### シナリオ4: 間違って修正した内容を元に戻したり、取り消しを取り消したい時
- **状況**: コードを修正中に大きなミスをして以前の状態に戻したいが、ショートカットを間違えて押しすぎて過去に戻りすぎた。
- **操作**: ノーマルモードで `u` を複数回押して変更を前に戻します。戻しすぎた場合は `Ctrl + r` を押して再び前へ（やり直し）進めます。

### シナリオ5: 特定の変数名を探し、その単語を消すと同時に新しい名前を入力したい時
- **状況**: コードの中から `user.name` という変数を探して一度に消し、すぐに `user.fullName` としてタイピングを始めたい。
- **操作**: ノーマルモードで `/user.name` を入力し `Enter` を押して該当位置にジャンプします。その状態で `cw` を押すと、`user.name` という単語が消えると同時にすぐさま**インサートモード**に切り替わるので、スムーズに `user.fullName` とタイピングできます。
