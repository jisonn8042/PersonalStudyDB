# Git 強制プッシュ(Force Push) 安全ガイド

## 概要
Gitで行った作業（特に `rebase` や `commit --amend` など、履歴を書き換える作業）をリモートリポジトリに反映させるためには、どうしても強制プッシュ（Force Push）が必要になる場合があります。今回はその際に使用できる `--force` と `--force-with-lease` の決定的な違いについて解説します。

---

## 💣 `--force` (無条件の上書き)
コマンド: `git push --force` (または `git push -f`)

通常の強制プッシュは、**リモートリポジトリの現在の状態を一切無視し、自分のPC（ローカル）の状態を丸ごと上書き**してしまいます。

### 🚨 起こりうる大惨事のシナリオ
1. 自分が作業を終え、リモートブランチにコードをアップします。
2. 自分が自分のコードを修正（`rebase` など）している間に、**チームメンバーのBさんが同じブランチに自分のコードをPushします。**
3. 私は追加の修正作業を終えた後、確認せずに `git push --force` を実行します。
4. **結果:** リモートリポジトリにあった**Bさんの追加コードが跡形もなくすべて消え去ってしまいます。** Gitは容赦なく自分のPCの状態だけで上書きしてしまうからです。

> 🚜 **例え:** 周りの状況を見ずにすべてを押し潰す **普通のブルドーザー**

---

## 🛡️ `--force-with-lease` (条件付きの上書き)
コマンド: `git push --force-with-lease`

このコマンドは、闇雲に上書きする前に裏側で**「自分が最後に確認した時のリモートリポジトリの状態から、今も変わっていないか？」**と一度確認するプロセスを経ます。

### ✨ どのように大惨事を防いでくれるのか？
先ほどと同じシナリオでこのコマンドを使用したらどうなるでしょうか？
* 自分が作業している間に、**Bさんや他の誰かがリモートに新しいコードをPushしていた場合**
  👉 Gitは自分が持っていない新しいコードがリモートに存在することを感知し、**強制プッシュを拒否(失敗)**します。他のチームメンバーが苦労して書いたコードを吹き飛ばしてしまうのを防いでくれます。
* 自分が作業している間に、**誰もそのブランチに変更を加えていない場合**
  👉 状態が変わっていない（安全である）ことを確認した後、本来の意図通り自分のローカルの最新状態でリモートを上書きします。

> 🤖 **例え:** 押し潰す前に、他の誰かが作業した形跡がないかサッと周りを確認する **スマートブルドーザー**

---

## 💡 結論および実務での推奨事項
実務では、他の人のコードを吹き飛ばしてしまう事故を根絶するため、特別な理由がない限り**「 `--force` 」の代わりに「 `--force-with-lease` 」を使用することを強く推奨します。**

```bash
# 常に以下のコマンドを使う習慣をつけましょう！
git push --force-with-lease origin <ブランチ名>
```
