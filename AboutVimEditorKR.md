# Vim 에디터 사용법 가이드 (Vim Editor Guide)

## 1. Vim의 4가지 모드 (Modes)
Vim은 다른 에디터들과 달리 여러 가지 '모드'를 가지고 있습니다.
- **일반 모드 (Normal Mode)**: Vim을 처음 실행했을 때의 기본 모드입니다. 커서 이동, 복사, 붙여넣기, 삭제 등의 명령을 수행합니다. (다른 모드에서 `ESC`를 누르면 항상 일반 모드로 돌아옵니다.)
- **입력 모드 (Insert Mode)**: 텍스트를 입력하는 모드입니다. 일반 모드에서 `i`, `a`, `o` 등을 누르면 진입합니다.
- **명령행 모드 (Command-line Mode)**: 파일 저장, 종료, 검색 및 치환, 환경 설정 등의 명령을 처리하는 모드입니다. 일반 모드에서 `:`을 누르면 화면 맨 아래에 프롬프트가 열리며 진입합니다.
- **비주얼 모드 (Visual Mode)**: 텍스트를 블록 지정하여 선택하는 모드입니다. 일반 모드에서 `v` (글자 단위), `V` (줄 단위), `Ctrl+v` (블록 단위)를 눌러 진입합니다.

---

## 2. 모드 전환 방법
- **일반 모드로 돌아가기**: `ESC` 또는 `Ctrl + [`
- **입력 모드로 진입**: 
  - `i`: 현재 커서 위치 앞에서부터 입력 (insert)
  - `I`: 현재 줄의 맨 앞(공백 제외)에서부터 입력
  - `a`: 현재 커서 위치 뒤에서부터 입력 (append)
  - `A`: 현재 줄의 맨 뒤에서부터 입력
  - `o`: 현재 줄 바로 아래에 새로운 빈 줄을 만들고 입력
  - `O`: 현재 줄 바로 위에 새로운 빈 줄을 만들고 입력

---

## 3. 커서 이동 (일반 모드)
방향키(←, ↓, ↑, →) 대신 아래 키를 사용하여 손을 키보드 중앙에 유지한 채 빠르게 이동할 수 있습니다.
- **기본 이동**: 
  - `h`: 좌
  - `j`: 하
  - `k`: 상
  - `l`: 우
- **단어 단위 이동**:
  - `w`: 다음 단어의 첫 글자로 이동
  - `b`: 이전 단어의 첫 글자로 이동
  - `e`: 단어의 마지막 글자로 이동
- **줄 내 이동**:
  - `0`: 현재 줄의 맨 앞으로 이동
  - `^`: 현재 줄의 첫 번째 공백이 아닌 문자로 이동
  - `$`: 현재 줄의 맨 뒤로 이동
- **문서 내 이동**:
  - `gg`: 문서의 맨 첫 줄로 이동
  - `G`: 문서의 맨 마지막 줄로 이동
  - `[숫자]G`: 입력한 숫자 번째 줄로 이동 (예: `50G` - 50번째 줄로 이동)

---

## 4. 텍스트 편집 (삭제, 복사, 붙여넣기, 치환)
- **삭제 및 잘라내기 (Delete)**:
  - `x`: 현재 커서가 위치한 문자 하나 삭제
  - `dd`: 현재 줄 전체 삭제 (잘라내기에 해당하여 나중에 붙여넣을 수 있음)
  - `dw`: 현재 커서 위치부터 단어 끝까지 삭제
  - `d$`: 현재 커서 위치부터 줄 끝까지 삭제
- **복사 (Yank)**:
  - `yy`: 현재 줄 전체 복사
  - `yw`: 현재 위치부터 단어 끝까지 복사
- **붙여넣기 (Put)**:
  - `p`: 커서 다음(오른쪽 문자 또는 아래 줄)에 붙여넣기
  - `P`: 커서 이전(왼쪽 문자 또는 위 줄)에 붙여넣기
- **변경 (Change - 삭제 후 바로 입력 모드로 전환)**:
  - `cw`: 단어 끝까지 삭제하고 입력 모드로 전환
  - `cc`: 현재 줄 전체를 삭제하고 입력 모드로 전환

---

## 5. 실행 취소 및 재실행
- `u`: 방금 한 작업 취소 (Undo)
- `Ctrl + r`: 취소한 작업 다시 실행 (Redo)

---

## 6. 검색 및 치환
- **검색**:
  - `/검색어`: 현재 커서 위치에서 아래 방향으로 '검색어' 찾기
  - `?검색어`: 현재 커서 위치에서 위 방향으로 '검색어' 찾기
  - `n`: 다음 검색 결과로 이동 (검색 방향 유지)
  - `N`: 이전 검색 결과로 이동 (검색 방향 반대)
- **치환 (명령행 모드)**:
  - `:s/old/new/g`: 커서가 있는 현재 줄에서 모든 'old' 문구를 'new' 단어로 치환
  - `:%s/old/new/g`: 파일 전체에서 모든 'old' 문구를 'new' 단어로 치환
  - `:%s/old/new/gc`: 파일 전체에서 치환을 진행하되, 각 치환마다 확인(y/n)을 받음

---

## 7. 화면 제어 및 기타
- **화면 스크롤**:
  - `Ctrl + f`: 한 화면 아래로 스크롤 (Forward / Page Down)
  - `Ctrl + b`: 한 화면 위로 스크롤 (Backward / Page Up)
  - `Ctrl + d`: 반 화면 아래로 스크롤 (Down)
  - `Ctrl + u`: 반 화면 위로 스크롤 (Up)

---

## 8. 저장 및 종료 (명령행 모드)
- `:w`: 변경 사항 저장 (write)
- `:q`: 종료 (quit, 변경된 사항이 있으면 종료되지 않음)
- `:wq`: 저장하고 종료
- `:x`: 저장하고 종료 (`:wq`와 동일하지만 파일이 수정된 경우에만 저장 작업을 수행함)
- `:q!`: 수정된 변경 사항을 무시하고 강제로 종료

---

## 9. 실무 활용 시나리오 (Practical Scenarios)

### 상황 1: 텍스트 파일 전체에서 특정 단어를 일괄 변경해야 할 때
- **상황**: 문서나 코드 전체에 있는 'apple'이라는 단어를 모두 'banana'로 한 번에 바꾸고 싶다.
- **조작법**: `ESC`를 눌러 일반 모드인지 확인한 후, `:%s/apple/banana/g` 를 입력하고 `Enter`를 누릅니다.

### 상황 2: 현재 줄을 복사해서 여러 번 연속으로 붙여넣어야 할 때
- **상황**: 설정값이나 비슷한 형태의 코드를 여러 개 만들어야 해서, 지금 있는 줄을 복사해 아래에 3줄 더 붙여넣고 싶다.
- **조작법**: 일반 모드에서 `yy`를 눌러 현재 줄을 복사한 뒤, `p`를 3번 누르거나 `3p`를 입력하여 3번 붙여넣습니다.

### 상황 3: 커서를 이동하면서 단어들을 빠르게 뭉텅이로 지우고 싶을 때
- **상황**: 현재 커서 위치부터 뒤에 있는 3개의 단어(변수명 등)가 불필요하여 한 번에 삭제하고 싶다.
- **조작법**: 일반 모드에서 `3dw`를 누르면 현재 커서 위치부터 3개의 단어가 깔끔하게 지워집니다.

### 상황 4: 잘못 수정한 내용을 되돌리거나, 취소를 취소하고 싶을 때
- **상황**: 코드를 수정하다가 큰 실수를 해서 이전 상태로 되돌리고 싶은데, 단축키를 잘못 눌러서 너무 과거로 돌아가 버렸다.
- **조작법**: 일반 모드에서 `u`를 여러 번 눌러 변경 사항을 이전으로 되돌립니다. 너무 많이 되돌렸다면 `Ctrl + r`을 눌러 다시 앞으로(재실행) 되돌립니다.

### 상황 5: 특정 변수명을 찾아 해당 단어를 지우는 동시에 새로운 이름으로 타이핑하고 싶을 때
- **상황**: 코드에서 `user.name`이라는 변수를 찾아 한 번에 지우고, 곧바로 `user.fullName`으로 타이핑을 시작하고 싶다.
- **조작법**: 일반 모드에서 `/user.name`을 입력하고 `Enter`를 쳐서 해당 위치로 점프합니다. 그 상태에서 `cw`를 누르면 `user.name` 단어가 지워지면서 즉시 **입력 모드**로 전환되므로, 자연스럽게 `user.fullName`을 타이핑할 수 있습니다.
