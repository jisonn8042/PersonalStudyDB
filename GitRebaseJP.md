# Git Rebase 実践ガイド

## 概要
`git rebase`は、ブランチのベース（開始点）を他のコミットに移動させたり、コミット履歴をきれいに整理したりする際に使用する強力なコマンドです。このドキュメントでは、実務ですぐに使える**コアな使い方と実際のシチュエーション**について解説します。

## 🚨 Rebaseの第一原則 (The Golden Rule)
> **「すでにリモート(Remote)にPushされ、他の人と共有されているブランチでは絶対にRebaseを行わないこと」**
* Rebaseはコミットのハッシュ（時点）を完全に書き換えるため、共有されているブランチをRebaseした後に強制プッシュ（`push -f`）すると、他のチームメンバーの作業に深刻な影響を与える可能性があります。
* **いつ使うべきか？** 「自分だけが一人で作業しているローカルブランチ」を整理したり、最新化したりする場合にのみ使用します。

---

## 実践シチュエーション1: 元ブランチの最新の変更を自分のブランチに取り込む時 (Rebase)
`merge`を使用すると不要な「Merge commit」が作成され、ブランチの履歴がクモの巣のように複雑になります。`rebase`を使用すると、自分が作業したコミットが最新のメインブランチの上にきれいに一直線に並びます。

### ❓ 状況:
`main`ブランチから分岐して`feature/login`で作業中です。その間に別のチームメンバーが`main`に新しい機能を追加しました。自分のコードが最新の`main`でも正常に動作するかを事前に確認し、コミットの線を一直線に揃えたい場合。

### 💻 方法:
```bash
# 1. mainブランチを最新にアップデート
git checkout main
git pull 

# 2. 自分の作業ブランチに移動
git checkout feature/login

# 3. mainブランチをベースにしてリベースを実行！
git rebase main
```

**💡 競合（Conflict）が発生したら？**
1. エディタを開き、競合が発生したファイルを手動で修正（Resolve）します。
2. 修正したファイルをステージングします: `git add <修正したファイル>`
3. ⚠️ **注意:** コミットを別途行わず、リベースを継続します。`git rebase --continue`
* *(参考: 競合が複雑すぎて諦めたい場合は、`git rebase --abort`を入力すると、開始前の状態に完全に戻ります。)*

---

## 実践シチュエーション2: ごちゃごちゃした自分のローカルコミットを一つにきれいにまとめる時 (Interactive Rebase)
「テストコード追加」「タイポ修正」「本当の修正」「あっ、また修正」のような細かいコミットを、PR（Pull Request）を作成する前に意味のある一つのコミットに圧縮したい場合に使用します。

### ❓ 状況:
自分の`feature`ブランチにある直近の3つのコミットを一つにきれいにまとめたい場合。

### 💻 方法:
```bash
# 直近3つのコミットを対象にインタラクティブリベースを実行
git rebase -i HEAD~3
```
このように入力すると、エディタが開き、以下のような内容が表示されます。（上が古いコミットです。）
```text
pick 1a2b3c4 機能開発の第1段階完了
pick 5d6e7f8 (タイポ修正) 変数名のタイポを修正
pick 9g0h1i2 (テスト) コンソールログを削除
```

**コミットをまとめる (`squash` / `s`):**
残しておきたい最初の（最も古い）コミットは`pick`のままにし、まとめたい下のコミットの前の`pick`を`s`（または`squash`）に変更します。
```text
pick 1a2b3c4 機能開発の第1段階完了
s 5d6e7f8 (タイポ修正) 変数名のタイポを修正
s 9g0h1i2 (テスト) コンソールログを削除
```
保存して画面を閉じると、3つのコミットメッセージを一つにまとめるための新しい編集画面が表示されます。不要なメッセージは削除し、代表的なメッセージに整えた上で、保存して終了します。

---

## 実践シチュエーション3: 以前のコミットの内容を少し修正したい時 (Interactive Rebase - Edit)
数個前のコミットでファイルを一つ入れ忘れたり、コードを少し修正して挿入したい時に便利です。

### ❓ 状況:
2つ前のコミットでファイルを一つ誤って入れ忘れました。新しいコミットを作成せず、過去のそのコミットにスッと入れたい場合。

### 💻 方法:
```bash
# 直近2つのコミットを対象にインタラクティブリベースを実行
git rebase -i HEAD~2
```

エディタが開いたら、修正したいコミットの前の`pick`を`edit`または`e`に変更して保存します。
```text
e 3b4c5d6 ログ追加
pick 7e8f9g0 スタイル修正
```

するとGitは時間が遡り、`3b4c5d6`のコミットが完了した直後の時点で停止します。
```bash
# ファイルの追加およびステージング
git add 入れ忘れたファイル.txt

# 該当するコミットを上書き（メッセージを維持する場合は --no-edit, 変更する場合は -m "新しいメッセージ"）
git commit --amend --no-edit

# リベースを引き続き進行（タイムマシンに乗って再び元の最新時点に戻る）
git rebase --continue
```

---

## 🛠 Rebase後にリモートへPushする際の注意点 (Force Push)
Rebaseを行うと、コミットの**「親（以前のコミット）」が変わるため、コードが同じであっても全く新しいコミットハッシュ（固有番号）が生成**されます。

* **リモート(Remote):** Rebaseする前の過去のコミット(A)を記憶している
* **ローカル(Local):** Rebaseされて親が変わった新しいコミット(A')を持っている

もし作業していたブランチをすでにリモートリポジトリにPushしている状態で、自分のPC（ローカル）でのみRebaseを行った場合、Gitから見れば「あれ？リモートに保存されていたコミット(A)がローカルにはないぞ？互いの歴史が分かれてしまった！」と認識し、データを失うことを恐れて通常のPush（`git push`）を拒否（失敗）します。

この時は、自分が意図的に履歴をきれいに上書きしようとしているのが事実なので、**強制プッシュ(Force Push)**でリモートリポジトリに知らせる必要があります。

```bash
# 💡 Tips: --force の代わりに --force-with-lease を使いましょう！
# 自分がプッシュした後に、他のチームメンバーの変更がリモートに発生していない場合にのみ安全に上書きします。
git push --force-with-lease origin feature/login
```

---

## 💡 要約 (チートシート)
* **最新基準に合わせる:** `git fetch` -> `git checkout 自分のブランチ` -> `git rebase origin/main` (または `git rebase main`)
* **コミットをまとめる/修正する:** `git rebase -i HEAD~N` (Nは確認するコミット数)
* **競合解決後に続行:** 解決 -> `git add .` -> `git rebase --continue`
* **失敗した時のキャンセル:** `git rebase --abort` (これ一つでいつでもリベース開始前に脱出可能！)
