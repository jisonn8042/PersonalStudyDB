# Git Rebase 실전 가이드

## 개요
`git rebase`는 브랜치의 베이스(시작점)를 다른 커밋으로 옮기거나, 커밋 히스토리를 깔끔하게 정리할 때 사용하는 강력한 명령어입니다. 이 문서는 실무에서 바로 써먹을 수 있는 **핵심 사용법과 실제 상황**을 다룹니다.

## 🚨 Rebase 제1원칙 (The Golden Rule)
> **"이미 원격(Remote)에 Push되어 다른 사람과 공유 중인 브랜치에서는 절대 Rebase를 하지 않는다."**
* Rebase는 커밋의 해시(시점)를 완전히 새로 쓰기 때문에, 공유된 브랜치를 Rebase한 후 강제 푸시(`push -f`)하면 다른 팀원들의 작업이 심각하게 꼬일 수 있습니다.
* **언제 써야 하나요?** 오직 "나만 혼자 쓰고 있는 로컬 브랜치"를 정리하거나 최신화할 때 사용합니다.

---

## 실전 사용 상황 1: 원본 브랜치의 최신 변경사항을 내 브랜치에 가져올 때 (Rebase)
`merge`를 사용하면 불필요한 "Merge commit"이 생기며 브랜치 히스토리가 거미줄처럼 복잡해집니다. `rebase`를 쓰면 내가 작업한 커밋들이 최신 메인 브랜치 위로 깔끔하게 한 줄로 올라갑니다.

### ❓ 상황:
`main` 브랜치에서 분기하여 `feature/login`에서 작업 중입니다. 그 사이 다른 팀원이 `main`에 새로운 기능을 추가했습니다. 내 코드가 최신 `main`에서도 잘 도는지 미리 확인하고, 커밋 선을 한 줄로 맞추고 싶습니다.

### 💻 방법:
```bash
# 1. main 브랜치를 최신으로 업데이트
git checkout main
git pull 

# 2. 내 작업 브랜치로 이동
git checkout feature/login

# 3. main 브랜치를 베이스로 리베이스 진행!
git rebase main
```

**💡 충돌(Conflict)이 났다면?**
1. 에디터를 열어 충돌이 난 파일들을 직접 수정(Resolve)합니다. 
2. 수정한 파일을 스테이징합니다: `git add <수정한 파일>`
3. ⚠️ **주의:** 커밋을 따로 하지 말고 리베이스를 이어갑니다. `git rebase --continue`
* *(참고: 충돌이 너무 복잡해서 포기하고 싶을 땐 `git rebase --abort`를 치면 시작 전으로 완전히 되돌아갑니다.)*

---

## 실전 사용 상황 2: 지저분한 내 로컬 커밋들을 하나로 예쁘게 합칠 때 (Interactive Rebase)
"테스트 코드 추가", "오타 수정", "진짜 수정", "아앗 또 수정" 처럼 자잘한 커밋들을 PR(Pull Request) 올리기 전에 의미 있는 하나의 커밋으로 압축하고 싶을 때 사용합니다.

### ❓ 상황:
내 `feature` 브랜치에 있는 최근 3개의 커밋을 하나로 깔끔하게 합치고 싶습니다.

### 💻 방법:
```bash
# 최근 3개의 커밋을 대상으로 인터랙티브 리베이스 실행
git rebase -i HEAD~3
```
이렇게 치면 편집기가 열리면서 아래와 같은 내용이 나옵니다. (위쪽이 오래된 커밋입니다.)
```text
pick 1a2b3c4 기능 개발 1단계 완료
pick 5d6e7f8 (오타 수정) 변수명 오타 수정
pick 9g0h1i2 (테스트) 콘솔 로그 지움
```

**커밋 합치기 (`squash` / `s`):**
남겨둘 첫 번째(가장 오래된) 커밋은 `pick`으로 두고, 합치고자 하는 아래의 커밋들 앞의 `pick`을 `s` (또는 `squash`)로 바꿉니다.
```text
pick 1a2b3c4 기능 개발 1단계 완료
s 5d6e7f8 (오타 수정) 변수명 오타 수정
s 9g0h1i2 (테스트) 콘솔 로그 지움
```
저장하고 창을 닫으면, 세 커밋의 메시지를 하나로 합칠 수 있는 다시 새로운 편집창이 뜹니다. 불필요한 메시지는 지우고, 대표 메시지로 다듬은 뒤 저장하고 종료합니다.

---

## 실전 사용 상황 3: 이전 커밋의 내용을 살짝 수정하고 싶을 때 (Interactive Rebase - Edit)
몇 개 전의 커밋에 파일을 하나 빠뜨렸거나, 코드를 잠깐 수정해서 끼워넣고 싶을 때 유용합니다.

### ❓ 상황:
2개 전의 커밋에 파일 하나를 실수로 안 넣었습니다. 새 커밋을 만들지 않고 과거의 그 커밋에 쏙 넣고 싶습니다.

### 💻 방법:
```bash
# 최근 2개의 커밋을 대상으로 인터랙티브 리베이스 실행
git rebase -i HEAD~2
```

편집기가 열리면, 수정하고 싶은 커밋 앞의 `pick`을 `edit` 또는 `e`로 변경하고 저장합니다.
```text
e 3b4c5d6 로그 추가
pick 7e8f9g0 스타일 수정
```

그러면 Git은 시간이 거꾸로 흘러 `3b4c5d6` 커밋이 완료된 직후 시점으로 되돌아가서 멈춥니다.
```bash
# 파일 추가 및 스테이징
git add 빠뜨린파일.txt

# 해당 커밋을 덮어씀 (메시지 유지 시 --no-edit, 변경 시 -m "새 메시지")
git commit --amend --no-edit

# 리베이스 계속 진행 (타임머신을 타고 다시 원래의 최신 시점으로 돌아옴)
git rebase --continue
```

---

## 🛠 Rebase 후 원격에 Push 할 때 주의사항 (Force Push)
Rebase를 하면 커밋의 **'부모(이전 커밋)'가 바뀌기 때문에, 코드가 같아도 완전히 새로운 커밋 해시(고유번호)가 생성**됩니다.

* **원격(Remote):** Rebase 하기 전 과거의 커밋(A)을 기억하고 있음
* **로컬(Local):** Rebase되어 부모가 바뀐 새로운 커밋(A')을 가지고 있음

만약 작업하던 브랜치를 이미 원격 저장소에 Push해둔 상태에서 내 컴퓨터(로컬)에서만 Rebase를 진행했다면, Git 입장에서는 "어? 원격에 저장되어 있던 커밋(A)이 내 로컬에는 없네? 서로 역사가 갈라졌잖아!"라고 인식하여 데이터를 잃을까 봐 일반적인 Push(`git push`)를 거부(실패)하게 됩니다.

이때는 내가 의도적으로 히스토리를 깔끔하게 덮어쓰려는 것이 맞으므로 **강제 푸시(Force Push)**로 원격 저장소에 알려주어야 합니다.

```bash
# 💡 꿀팁: --force 대신 --force-with-lease 를 사용하세요!
# 내가 푸시한 이후 다른 팀원의 변경사항이 원격에 생기지 않았을 때만 안전하게 덮어씁니다.
git push --force-with-lease origin feature/login
```

---

## 💡 요약 (치트시트)
* **최신 기준 맞추기:** `git fetch` -> `git checkout 내브랜치` -> `git rebase origin/main` (또는 `git rebase main`)
* **커밋 합치기/수정하기:** `git rebase -i HEAD~N` (N은 확인할 커밋 개수)
* **충돌 해결 후 계속:** 해결 -> `git add .` -> `git rebase --continue`
* **망했을 때 취소:** `git rebase --abort` (이거 하나면 언제든 리베이스 시작 전으로 탈출 가능!)
