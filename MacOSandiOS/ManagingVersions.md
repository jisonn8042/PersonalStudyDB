# iOS開発環境のバージョン管理ガイド

開発をしながら２つの同じプログラムでバージョンが違うものを設置する場合があります。

*例）パソコンには既にRuby 2.6.10が設置されているのに、使おうと思ったライブラリがRuby 3.2.5を必要となっている場合*

上の状況みたいに開発環境の要素を揃わないで協業を行うと、自分のPCでは動くのに、チームメンバーのPCでは動かないことがありました。

どうすれば開発環境を一致させることができるかな、実際に手を動かしながら学んだ **「チーム開発で事故らないための環境管理」** について共有したいと思います。

</br>
</br>
</br>

開発環境をチーム全員で統一するには、以下の要素をプロジェクト単位で固定（ロック）する必要があります。

下の項目は開発環境を一致させるために管理が必要な項目です。

**管理項目**

1. Ruby言語バージョン

2. Rubyツール別のバージョン（cocoapods, bundler, ActiveSupport等）

3. iOSライブラリバージョン

4. xcodeバージョン

5. システムツール設置管理(git, swiftlint等)

6. テスト環境SettingでのExcluded Architecture項目

</br>

**選択事項**

1. OSバージョン

2. xcodeprojファイル管理（また、知識が足りないためこの内容に関しては今後作成予定）

</br>
</br>
</br>

目次です、以下の手順に説明します。

**Ⅰ. バージョンを作成するために必要なこと**

1. Homebrewの設置

2. rbenv,ruby-buildの設置

</br>
</br>
</br>

**Ⅱ. バージョン作成**

1. Ruby言語バージョ作成（.ruby-versionファイル作成）

    1.1. .ruby-versionファイル作成

    1.2. Rubyの設置（特定バージョンを使う場合）

2. Rubyツール別のバージョン作成（Gemfileファイル作成）

    2.1. Gemfileファイル作成
   
    2.2. bundlerの設置（特定バージョンを使う場合）

    2.3. cocoapodsの設置（特定バージョンを使う場合）

3. iOSライブラリバージョン作成（Podfileファイル作成）

    3.1. Podfileファイル作成

4. xcodeバージョン作成（.xcode-versionファイル作成）

    4.1. .xcode-versionファイル作成

5. システムツール設置管理(git, swiftlint等)（Brewfileファイル作成）

    5.1. Brewfileファイルを作成

6. テスト環境SettingでのExcluded Architecture項目作成（README.mdファイル作成）

</br>
</br>
</br>

**Ⅲ. バージョンセットアップ**

1. brew bundle install（システムツールセットアップ）

    1.1. rbenv install [version] (ruby-versionファイルを確認し同じバージョンを設置)

2. bundle install（特定されているRubyツールを設置（cocoapodsなど））

3. bundle exec pod intall（特定されているcocoapodsバージョンでiOSライブラリ設置）

4. .xcode-version, README.mdファイル参考（他に気にすべき項目があるか確認）

</br>
</br>
</br>

**Ⅳ. 【番外編】バージョン管理の判断基準**

</br>
</br>
</br>

## Ⅰ. バージョンを作成するために必要なこと

### 1. Homebrewの設置

Homebrewはシステムレベルのパッケージ管理ツールで運営体制に必要なパッケージを管理します。

例）Node.js,pythonなど

Macでは最初からRubyが設置されていますが、、、

特定バージョンのRubyが必要で設置すると、パソコンはどのバージョンのRubyを使えば良いのか分からなくなるのでバージョンんを管理してくれるツールが必要となります。

バージョンを管理するツールとしてrbenvを良く使われていますが、そのツールを設置するにはHomebrewが必要となるので、まず、Homebrewを設置します。

</br>
</br>
</br>

**設置する前に設置有無確認**

下のコマンドをターミナルに貼り付けてEnterを押してください。

結果が、Homebrew 4.6.20 の形式で出たら2. rbenv,ruby-buildの設置に進めてください。

```zsh
//設置されているHomebrewバージョン確認コマンド
brew -v
```

</br>
</br>
</br>

**設置方法**

Homebrew設置の流れ

1. Homebrewを設置（ターミナルにコマンド入力で実施）

2. パスワード入力

3. 設置確認（ターミナルにコマンド入力で実施）

</br>
</br>
</br>

**1. Homebrewを設置（ターミナルにコマンド入力で実施）**

Homebrewサイトに入ると、インストールコマンドを確認できます。
そのコマンドをターミナルターミナルに貼り付け（及手入力）Enterを押します。

```zsh
// Homebrewでコピしたコマンド（下のコマンドを貼り付けてもOK）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

</br>
</br>
</br>

**2. パスワード入力**

その後パスワード入力が出た時、Macのパスワードを入力し待つと設置が完了されます。

</br>
</br>
</br>

**3. 設置確認**

下のコマンドをターミナルに貼り付けてEnterを押して結果が、Homebrew 4.6.20 の形式で出たら成功

```zsh
//設置されているHomebrewバージョン確認コマンド
brew -v
```

</br>
</br>
</br>

+ 自分の場合（M1使用中）設置した後にコマンド入力が必要となっていて入力しました。

（何でこの過程が必要となるのか、他のポストで記述します。（Mechanismofrbenv））

```zsh
// /Users/ユーザ名/.zprofile ファイルに eval "$(/opt/homebrew/bin/brew shellenv)"を作成します。
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> /Users/ユーザ名/.zprofile

// 実際に実行
eval "$(/opt/homebrew/bin/brew shellenv)"
```

</br>
</br>
</br>

---

</br>
</br>
</br>

### 2. rbenv,ruby-buildの設置

rbenvはRubyバージョンを管理するツールとして良く使われています。

プロジェクト別に使用するRubyのバージョンを制御することができます。

まずは、rbenv,ruby-buildをインストールしてからどう制御できるか述します。

</br>

*rbenvとruby-build２つを設置する理由*

*rbenvはバージョンを切り替えるツールで他のバージョンのRubyを設置できないのですが、ruby-buildを設置することで他のバージョンのRubyを設置することが出来ます。*

</br>
</br>
</br>

**設置方法**

rbenv、ruby-version設置の流れ

1. rbenv,ruby-versionを設置する（ターミナルにコマンド入力で実施）

2. ~/.zshrc ファイルにrbenv設定技術する（ターミナルにコマンド入力で実施）

3. 2で技術した設定を現在のターミナルに適用（ターミナルにコマンド入力で実施）

4. 設置確認（ターミナルにコマンド入力で実施）

</br>
</br>
</br>

**1. rbenv,ruby-versionを設置する（ターミナルにコマンド入力で実施）**

Homebrewを活用してrbenv、ruby-buildを設置します。

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//rbenvを設置するコマンドです。ターミナルに貼り付けてEnterを押すと設置されます。
brew install rbenv ruby-build
```

</br>
</br>
</br>

**2. ~/.zshrc ファイルにrbenv設定技術する（ターミナルにコマンド入力で実施）**

rbenvは色んな違うバージョンのRubyを正しいバージョンで実行するよう管理してくれるツールで、

rbenvを活用するにはターミナルが実行される度にある設定をする必要があります。

~/.zshrcに設定（eval "$(rbenv init - zsh)"）を追加することで、

毎回ターミナルが実行すると設定をするようにできます。

（詳しいrbenvの仕組みについては他のポストで説明します。（Mechanismofrbenv））

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
// ~/.zshrcファイルにeval "$(rbenv init -zsh)"を作成します。
echo 'eval "$(rbenv init -zsh)"' >> ~/.zshrc
```

</br>
</br>
</br>

**3. 2で技術した設定を現在のターミナルに適用（ターミナルにコマンド入力で実施）**

上でターミナルが実行される度にある設定をするようにしたのですが、

現在のターミナルにはその設定が実行されていないのでコマンドで直接入力する必要があります。

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//　~/.zshrcを実行するコマンド
source ~/.zshrc
```

</br>
</br>
</br>

**4. 設置確認（ターミナルにコマンド入力で実施）**

最後はちゃんとrbenvが設置されているのかrbenvの設置されているバージョンを確認するコマンドで確認してみます。

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置されているrbenvのバージョンを見せるコマンド
rbenv -v
```

</br>
</br>
</br>

---

</br>
</br>
</br>

### 2.1. Rubyの設置（特定バージョンを使う場合）

特定バージョンを使わない場合3. bundlerの設置に進んでも大丈夫です。

Rubyの他のバージョン使うため設置するときrbenvを活用して設置することができます。

</br>
</br>
</br>

**設置する前に設置有無確認**

下のコマンドをターミナルに貼り付けてEnterを押してください。

結果が、設置したバージョンがある場合、

設置しようとしたRubyバージョンが既に設置しているので3. bundlerの設置に進めてください。

```zsh
//設置されているRubyバージョン確認コマンド
rbenv versions
```

</br>
</br>
</br>

**設置方法**

1. 設置可能なバージョン確認（ターミナルにコマンド入力で実施）

2. 他のバージョンのRuby設置（ターミナルにコマンド入力で実施）

3. 設置確認（ターミナルにコマンド入力で実施）

</br>
</br>
</br>

**1. 設置可能なバージョン確認（ターミナルにコマンド入力で実施）**

Rubyバージョンの中で設置できるバージョンを確認します。

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置可能なバージョンを見せます。
rbenv install --list-all
```

</br>
</br>
</br>

**2. 他のバージョンのRuby設置（ターミナルにコマンド入力で実施）**

上のコマンドの出力結果として出たバージョンの中で設置したいバージョンを選択してください

その後、下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置可能なバージョンを見せます。
//入力例）rbenv install 3.3.4
rbenv install [あなたが設置したいバージョン]

```

</br>
</br>
</br>

**3. 設置確認（ターミナルにコマンド入力で実施）**

現在パソコンに設置されているRubyのバージョンを確認するコマンドで正常に設置されているのか確認します。

下のコマンドをターミナルに貼り付けてEnterを押した結果の中で設置したバージョンが含まれてと成功です。

```zsh
//設置されているRubyバージョンを見せます。
rbenv versions
```

</br>
</br>
</br>

---

</br>
</br>
</br>

### 3. bundlerの設置（基本的にRubyを設置するとそのバージョンに合うBundlerが設置されますが、特定バージョンを使う場合行ってください。）

基本的にRubyを設置するとそのバージョンに合うBundlerが設置されるので特定バージョンがいらない場合4. cocoapodsの設置に進んでも大丈夫です。

bundlerはRubyプロジェクトでRubyのライブラリを管理する為、使われているツールです。

</br>
</br>
</br>

**設置方法**

1. 設置可能なバージョン確認（ターミナルにコマンド入力で実施）

2. 他のバージョンのBundler設置（ターミナルにコマンド入力で実施）

3. 設置確認（ターミナルにコマンド入力で実施）

</br>
</br>
</br>

**1. 設置可能なバージョン確認（ターミナルにコマンド入力で実施）**

Bundlerバージョンの中で設置できるバージョンを確認します。

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置可能なバージョンを見せます。
gem search ^bundler$ --remote --all
```

</br>
</br>
</br>

**2. 他のバージョンのRuby設置（ターミナルにコマンド入力で実施）**

上のコマンドの出力結果として出たバージョンの中で設置したいバージョンを選択してください

その後、下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置可能なバージョンを見せます。
//入力例）gem install bundler -v 2.4.22
gem install bundler -v [あなたが設置したいバージョン]
```

</br>
</br>
</br>

**3. 設置確認（ターミナルにコマンド入力で実施）**

現在パソコンに経路にセットされているbundlerのバージョンを確認するコマンドで正常に設置されているのか確認します。

下のコマンドをターミナルに貼り付けてEnterを押した結果の中で設置したバージョンが含まれてと成功です。

```zsh
//セットされているbundlerバージョンを見せます。
gem list bundler
```

</br>
</br>
</br>

---

</br>
</br>
</br>

### 4. cocoapodsの設置

cocoapodsはiOSやmacOS開発で必要なライブラリを管理する為、使われているツールです。

</br>
</br>
</br>

**設置方法**

1. cocoapods設置（ターミナルにコマンド入力で実施）

2. 設置確認（ターミナルにコマンド入力で実施）

</br>
</br>
</br>

**1. cocoapodsを設置（ターミナルにコマンド入力で実施）**

上のコマンドの出力結果として出たバージョンの中で設置したいバージョンを選択してください

その後、下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//最新で安定的なバージョンを設置します。
gem install cooapods
```

</br>
</br>
</br>

**2. 設置確認（ターミナルにコマンド入力で実施）**

現在パソコンに経路にセットされているcocoapodsのバージョンを確認するコマンドで正常に設置されているのか確認します。

下のコマンドをターミナルに貼り付けてEnterを押した結果の中で設置したバージョンが含まれてと成功です。

```zsh
//セットされているcocoapodsバージョンを見せます。
pod --version
```

</br>
</br>
</br>

### 4.1. cocoapodsの設置（特定バージョンを使う場合）



</br>
</br>
</br>

**設置方法**

1. 設置可能なバージョン確認（ターミナルにコマンド入力で実施）

2. 他のバージョンのcocoapods設置（ターミナルにコマンド入力で実施）

3. 設置確認（ターミナルにコマンド入力で実施）

</br>
</br>
</br>

**1. 設置可能なバージョン確認（ターミナルにコマンド入力で実施）**

cocoapodsバージョンの中で設置できるバージョンを確認します。

下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置可能なバージョンを見せます。
gem search ^cocoapods$ --all
```

</br>
</br>
</br>

**2. 他のバージョンのRuby設置（ターミナルにコマンド入力で実施）**

上のコマンドの出力結果として出たバージョンの中で設置したいバージョンを選択してください

その後、下のコマンドをターミナルに貼り付けてEnterを押してください。

```zsh
//設置可能なバージョンを見せます。
//入力例）rbenv install 3.3.4
rbenv install [あなたが設置したいバージョン]

```

</br>
</br>
</br>

**3. 設置確認（ターミナルにコマンド入力で実施）**

現在パソコンに設置されているRubyのバージョンを確認するコマンドで正常に設置されているのか確認します。

下のコマンドをターミナルに貼り付けてEnterを押した結果の中で設置したバージョンが含まれてと成功です。

```zsh
//設置されているRubyバージョンを見せます。
rbenv versions
```

</br>
</br>
</br>
