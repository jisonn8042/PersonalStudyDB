# Git 강제 푸시(Force Push) 안전 가이드

## 개요
Git에서 진행한 작업(특히 `rebase`, `commit --amend` 등 기록을 덮어쓰는 작업)을 원격 저장소에 올리기 위해서는 불가피하게 강제 푸시가 필요할 때가 있습니다. 이때 사용할 수 있는 `--force`와 `--force-with-lease`의 결정적인 차이점을 알아봅니다.

---

## 💣 `--force` (무조건 덮어쓰기)
명령어: `git push --force` (또는 `git push -f`)

일반적인 강제 푸시는 **원격 저장소의 현재 상태를 무시하고 내 컴퓨터(로컬)의 상태로 통째로 덮어씌워 버립니다.**

### 🚨 발생할 수 있는 대참사 시나리오
1. 내가 작업을 마치고 원격 브랜치에 코드를 올립니다.
2. 내가 내 코드를 추가로 수정(`rebase` 등)하는 동안, **팀원인 철수가 같은 브랜치에 자신이 작성한 추가 코드를 Push 합니다.**
3. 나는 내 수정 작업을 마친 뒤 생각 없이 `git push --force`를 실행합니다.
4. **결과:** 원격 저장소에 추가로 올라가 있던 **철수의 코드가 쥐도 새도 모르게 모두 날아가 버립니다.** Git은 무자비하게 내 컴퓨터의 최신 상태로만 덮어쓰기 때문입니다.

> 🚜 **비유:** 앞뒤 보지 않고 다 밀어버리는 **일반 불도저**

---

## 🛡️ `--force-with-lease` (조건부 덮어쓰기)
명령어: `git push --force-with-lease`

이 명령어는 무작정 덮어쓰기 전에 화면 뒤에서 **"내가 마지막으로 확인했던 원격 저장소의 상태가 지금도 그대로인가?"** 하고 한 번 확인(검사)하는 과정을 거칩니다.

### ✨ 어떻게 대참사를 막아주나요?
똑같은 시나리오에서 이 명령어를 사용한다면 어떻게 될까요?
* 내가 작업하는 동안 **철수나 다른 누군가가 원격에 새로운 코드를 올렸다면?**
  👉 Git은 내가 모르는 새로운 코드가 원격에 존재한다는 것을 감지하고, **강제 푸시를 거부(실패)**합니다. 다른 팀원이 땀 흘려 작성한 코드를 날려먹는 대참사를 막아줍니다.
* 내가 작업하는 동안 **아무도 그 브랜치에 코드를 올리지 않았다면?**
  👉 "안전하군!" 원격 저장소가 여전히 예전 상태 그대로인 것을 확인한 후, 원래 내 의도대로 안전하게 내 컴퓨터의 상태로 덮어씁니다.

> 🤖 **비유:** 다 밀어버리기 전에 혹시 남이 작업해둔 게 있는지 한 번 슥 눈치 보고 밀어버리는 **스마트 불도저**

---

## 💡 결론 및 실무 권장사항
실무에서는 팀원의 코드를 날려버리는 큰 실수를 원천 차단하기 위해, 이유를 막론하고 **무조건 일반 `--force` 대신 `--force-with-lease`를 사용하는 것을 강력하게 권장**합니다.

```bash
# 항상 아래 명령어를 습관화하세요!
git push --force-with-lease origin <브랜치명>
```
