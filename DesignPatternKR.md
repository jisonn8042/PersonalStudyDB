# 디자인 패턴이란 

“상태와 데이터 변화에 따라 객체들이 어떻게 생성·연결·소통·책임 분담할지를 정리한 설계 언어”

디자인 패턴은 데이터, 상태, 객체, 책임이 변하거나 복잡해질 때
객체들이 생성되고, 연결되고, 상호작용하는 방식을
유지보수하기 좋게 정리하기 위한 설계 방법이다.

## 설명

### 생성 패턴

객체를 누가, 언제, 어떤 조건으로 만들지 정리하는 패턴

생성 패턴은 객체를 직접 만들지 않고,
생성 책임과 생성 규칙을 분리해서
상황에 맞는 객체를 안정적으로 만들기 위한 패턴이다.

**Singleton** : 앱 전체에서 하나의 인스턴스 사용

**Factory** : 조건에 맞는 객체 생성

**Builder**: 복잡한 객체를 단계적으로 생성

---

### 구조 패턴

객체들을 어떻게 연결하고 감싸고 조합할지 정리하는 패턴

구조 패턴은 기존 객체들을 변경하지 않거나 최소한으로 변경하면서,
객체들을 조합하고 연결해서 더 큰 기능을 만들기 위한 패턴이다.

**Adapter** : 맞지 않는 인터페이스를 맞춰줌

**Facade** : 복잡한 내부 구조를 단순한 입구로 감쌈

**Decorator** : 기존 객체에 기능을 덧붙임

---

### 행위 패턴

객체들이 어떻게 소통하고, 위임하고, 판단하고, 상태 변화에 반응할지 정리하는 패턴

행위 패턴은 객체들이 서로 어떻게 요청하고, 알리고, 위임하고,
책임을 나눌지 정리하는 패턴이다.

**Delegate** : 한 객체에게 처리를 위임

**Observer** : 여러 객체에게 변경을 알림

**Strategy** : 행동 알고리즘을 교체

**Command** : 실행할 동작을 객체로 분리

**State** : 상태에 따라 행동을 바꿈

## 판별법

### 생성 패턴
목적이 객체 생성을 숨기는 것이라면

#### 질문 리스트
- 객체 생성 책임이 여기 있어도 되는가?
- 생성 조건이 복잡한가?
- 환경별로 다른 객체가 필요한가?
- 생성 과정을 숨겨야 하는가?

---

### 구조 패턴
목적이 기존 객체를 감싸거나 연결하는 것이라면

#### 질문 리스트

- 기존 코드를 크게 바꾸지 않고 새 구조에 맞출 수 있는가?
- 복잡한 내부 구조를 단순한 인터페이스로 제공할 수 있는가?
- 기존 객체에 기능을 안전하게 추가할 수 있는가?

---

### 생성 패턴
목적이 객체 간 요청, 알림, 위임, 알고리즘 교체라면

#### 질문 리스트

- 누가 이벤트를 발생시키는가?
- 누가 처리해야 하는가?
- 여러 객체가 알아야 하는가?
- 행동 방식을 교체해야 하는가?
- 상태에 따라 동작이 달라지는가?