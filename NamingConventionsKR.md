# 요약

* 웹 주소나 일반문서 파일을 관리한다면? **케밥 케이스**
* 데이터나 파이썬 코딩을 한다면? **스네이크 케이스**
* 프로젝트 관련 폴더, 파일, 앱/프로그램 소스 코드를 짠다면? **카멜 케이스**
  * 변수명이나 함수명이라면? **lowerCamelCase**
  * 클래스, 폴더, 파일 이름이라면? **PascalCase**

## 1. 케밥 케이스 (kebab-case)
단어 사이를 -로 연결하며, 주로 웹(Web) 환경에서 사랑받습니다.

* **URL 주소**: 브라우저 주소창에서 공백 대신 가장 읽기 편하고 검색 엔진 최적화(SEO)에 유리합니다. (예: ://example.com)
* **HTML/CSS**: 태그의 ID나 클래스명을 지을 때 표준처럼 쓰입니다. (예: <div class="main-container">)
* **폴더/파일**: 윈도우, 맥, 리눅스 어디서든 파일명이 깨질 염려가 적어 범용적으로 쓰입니다.

## 2. 스네이크 케이스 (snake-case)
단어 사이를 _로 연결하며, 데이터베이스와 백엔드에서 주로 씁니다.

* **데이터베이스(DB)**: 테이블 이름이나 컬럼(열) 이름을 지을 때 대소문자 구분이 없는 DB 시스템이 많아 언더바를 표준으로 사용합니다. (예: user_id, created_at)
* **파이썬(Python)**: 파이썬 언어에서 변수나 함수 이름을 지을 때 권장되는 공식 스타일입니다. (예: def get_user_name():)
* **환경 설정**: 시스템 설정값이나 로그 파일명에 자주 쓰입니다.

## 3. 카멜 케이스 (camelCase / PascalCase)
단어 첫 글자를 대문자로 구분하며, 프로그래밍 코드 내부에서 가장 많이 쓰입니다.

* **자바스크립트/자바/C#**: 변수명이나 함수명은 첫 글자를 소문자로 시작하는 lowerCamelCase를 씁니다. (예: userName, totalPrice)
* **클래스명**: 객체 지향 프로그래밍에서 클래스 이름은 첫 글자도 대문자로 쓰는 PascalCase가 규칙입니다. (예: UserAccount, OrderManager)
* **폴더, 파일명**: 개발 프로젝트 내에서 소스 코드가 담긴 폴더를 구분할 때 자주 쓰입니다.
  * Q. 그럼 클래스와 폴더, 파일명에 모두 PascalCase를 쓰게되면 헷갈리지 않을까?
  * A. 오히려"이름이 같아야 찾기 쉽다
    * 폴더(PascalCase): UserProfile/ (노란색 폴더 아이콘) → "이 안에 관련된 로직들이 모여 있겠군."
    * 파일(PascalCase): UserProfile.tsx (파일 아이콘) → "이게 진짜 실행되는 메인 코드구나."
